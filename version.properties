# App Version Configuration
# VERSION_NAME is the single source of truth and is managed by release-please
# (the `x-release-please-version` annotation marks the token it bumps).
# VERSION_CODE is derived from VERSION_NAME in app/build.gradle.kts.
# Do NOT hand-edit the version here — merge the release-please PR to bump it.

VERSION_NAME=1.9.1 # x-release-please-version
